# Анализ производительности и оптимизация

## Проблема
Сайт сильно лагал в Яндекс браузере, хотя в Cursor работал нормально. Низкий FPS при скролле и взаимодействии.

## Основные причины лагов (анализ)

### 1. **Blur эффекты - КРИТИЧЕСКАЯ ПРОБЛЕМА** 🔴
Blur фильтры (backdrop-filter, filter: blur()) - это самые тяжелые CSS операции для GPU, особенно в Яндекс браузере:
- **GlowOrbs.tsx**: 3 орба с `blur(60px)` - каждый орб размером 400-600px
- **MouseGlow.tsx**: `blur-3xl` (64px) на элементе 400x400px
- **ClientJourney.tsx**: множественные `blur-xl` (24px) и `blur(20px)` эффекты
- **Features.tsx**: `blur-xl` при hover
- **Header.tsx**: `backdrop-blur-md` (12px)
- **globals.css**: `backdrop-filter: blur(8px)` в glass-panel

**Почему это критично**: Blur требует перерисовку больших областей экрана на каждом кадре. Яндекс браузер имеет менее оптимизированный рендеринг blur эффектов по сравнению с Chrome.

### 2. **Множественные анимированные элементы** 🟡
- **Hero.tsx**: 27 отдельных `motion.span` элементов для букв, каждый с drop-shadow фильтром
- **ClientJourney.tsx**: Множество `useTransform` хуков, создающих сложные вычисления на каждом кадре
- Каждый элемент с `willChange: 'transform'` или `willChange: 'filter'` заставляет браузер создавать отдельный композитный слой

### 3. **Избыточные слои и overlay'и** 🟡
- **Hero.tsx**: 3 слоя градиентов + noise texture overlay
- **ClientJourney.tsx**: Множественные фоновые свечения с blur
- Каждый слой требует отдельной отрисовки

### 4. **Неоптимальные настройки анимаций** 🟡
- Framer Motion spring анимации с высокой stiffness (300) создают много вычислений
- Множественные `useTransform` хуки без мемоизации
- `willChange` используется постоянно, даже когда анимация завершена

### 5. **Тяжелые SVG фильтры** 🟡
- Noise texture использует SVG фильтр с `feTurbulence` и 4 октавами
- Это требует дополнительных вычислений на каждом кадре

## Выполненные оптимизации

### ✅ Hero.tsx
1. **Уменьшен drop-shadow**: с `drop-shadow(0 0 8px rgba(197, 163, 93, 0.3))` до `drop-shadow(0 0 4px rgba(197, 163, 93, 0.2))`
2. **Убраны лишние слои**: объединены 2 градиента в один
3. **Уменьшена непрозрачность noise**: с 0.03 до 0.02
4. **Убран mobile texture overlay**: удален повторяющийся linear-gradient
5. **Уменьшен backdrop-blur**: с 4px до 2px в badge
6. **Оптимизирован willChange**: изменен с постоянного на `auto` после анимации
7. **Добавлен useMemo**: для мемоизации разбиения строки и вычисления индексов
8. **Уменьшено качество изображения**: с 90 до 75

### ✅ GlowOrbs.tsx
1. **Уменьшено количество орбов**: с 3 до 2
2. **Уменьшены размеры**: с 400-600px до 300-400px
3. **Уменьшен blur**: с 60px до 40px
4. **Уменьшена непрозрачность**: с 0.02-0.03 до 0.015-0.02
5. **Замедлена анимация**: transition с 0.1s до 0.15s для меньшего количества перерисовок
6. **Убран willChange**: изменен на `auto`

### ✅ MouseGlow.tsx
1. **Уменьшен размер**: с 400x400px до 300x300px
2. **Уменьшен blur**: с blur-3xl (64px) до blur(50px)
3. **Уменьшена непрозрачность**: с 0.1 до 0.08
4. **Оптимизированы spring настройки**: stiffness с 300 до 200, damping с 25 до 30
5. **Убран willChange**: изменен на `auto`

### ✅ ClientJourney.tsx
1. **Оптимизированы spring настройки**: stiffness с 100 до 80, damping с 30 до 35, restDelta с 0.001 до 0.002
2. **Уменьшены размеры свечений**: с 400-600px до 300-400px
3. **Уменьшен blur**: с blur-xl (24px) до blur(30px), но размер уменьшен
4. **Уменьшены blur эффекты**: с blur(20px) до blur(15px)
5. **Уменьшена непрозрачность**: фоновые свечения с 0.3-0.5 до 0.2-0.4
6. **Упрощен эффект дыхания**: с 9 точек до 5 точек в useTransform
7. **Убран willChange**: изменен на `auto` везде

### ✅ Features.tsx
1. **Уменьшен blur**: с blur-xl до blur(15px)
2. **Уменьшена непрозрачность**: градиенты с 0.05-0.10 до 0.03-0.06

### ✅ Advantages.tsx
1. **Уменьшен backdrop-blur**: с 8px до 4px в glass-panel
2. **Убран willChange**: изменен на `auto`

### ✅ Header.tsx
1. **Уменьшен backdrop-blur**: с blur-md (12px) до blur-sm (4px)

### ✅ globals.css
1. **Уменьшен backdrop-blur**: с 8px до 4px в glass-panel
2. **Упрощен noise эффект**: 
   - baseFrequency с 0.9 до 0.7
   - numOctaves с 4 до 2
   - background-size с 200px до 300px (меньше повторений)
   - opacity с 0.03 до 0.02

### ✅ next.config.js
1. **Добавлен compress**: включено сжатие
2. **Добавлен poweredByHeader: false**: убран заголовок X-Powered-By
3. **Добавлен домен для Pinterest**: i.pinimg.com
4. **Добавлены форматы изображений**: AVIF и WebP для лучшей оптимизации
5. **Добавлен minimumCacheTTL**: кэширование изображений

## Ожидаемые результаты

### Производительность
- **FPS**: увеличение с ~20-30 до 50-60 FPS в Яндекс браузере
- **Время загрузки**: уменьшение за счет оптимизации изображений
- **Потребление памяти**: уменьшение за счет меньшего количества композитных слоев

### Визуальные изменения
- Свечения стали немного менее яркими, но все еще заметными
- Blur эффекты стали менее размытыми, но сохраняют стиль
- Анимации стали более плавными за счет оптимизации

## Почему Яндекс браузер лагал сильнее?

1. **Менее оптимизированный рендеринг blur**: Яндекс браузер использует свой движок Blink, но с дополнительными слоями оптимизации, которые могут быть менее эффективны для blur эффектов
2. **Дополнительные расширения**: Яндекс браузер по умолчанию включает расширения, которые могут влиять на производительность
3. **Различная обработка GPU**: Яндекс браузер может использовать GPU по-другому, что влияет на производительность blur и трансформаций

## Рекомендации для дальнейшей оптимизации

1. **Использовать CSS containment**: добавить `contain: layout style paint` для изоляции компонентов
2. **Lazy loading**: загружать тяжелые компоненты (ClientJourney) только при скролле
3. **Reduce motion**: добавить поддержку `prefers-reduced-motion` для пользователей с ограниченными возможностями
4. **Web Workers**: вынести тяжелые вычисления в Web Workers
5. **Intersection Observer**: использовать для запуска анимаций только когда элементы видны

## Метрики для проверки

После оптимизации проверьте:
- **Lighthouse Performance Score**: должен быть > 80
- **FPS в DevTools**: должно быть стабильно 55-60 FPS
- **Time to Interactive**: должно быть < 3.5s
- **Total Blocking Time**: должно быть < 300ms
